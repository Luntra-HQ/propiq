{
  "name": "propiq",
  "version": "1.0.0",
  "description": "PropIQ - AI-Powered Real Estate Investment Analysis Platform",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "build": "convex deploy",
    "build:frontend": "echo '📦 Installing frontend dependencies...' && cd frontend && npm install && echo '🔨 Building frontend...' && npm run build:prod && echo '✅ Frontend build complete!'",
    "export:users": "tsx scripts/export-users-to-csv.ts",
    "export:segments": "tsx scripts/export-users-to-csv.ts --segment",
    "export:free": "tsx scripts/export-users-to-csv.ts --free"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "convex": "^1.31.2",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "tsx": "^4.19.2"
  }
}
